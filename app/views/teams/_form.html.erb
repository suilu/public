<%= simple_form_for @team do |f| %>
<div class="form-team">
        <%= f.input :title, input_html: { class: "form-control"} %>
</div>

<%= f.submit "创建项目", class: "btn btn-success", data: { disable_with: "Submitting..." } %>

<% end %>

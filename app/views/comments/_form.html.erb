
<%= simple_form_for([@project, @project.comments.build]) do |f|%>

  <div class="field">
      <%= f.text_area :body, class: "form-control" %>
  </div>
  <div class="comment">
  <%= f.submit "添加评论", class: "btn btn-success" %>
  </div>
<% end %>

<style >
  .comment{
    margin-top: 20px;
  }
</style>
